export const metadata = {
    title: "Author Profile | WriteFlow",
    description: "Explore blogs and insights from our authors on WriteFlow.",
  };
  
  export default function AuthorLayout({ children }) {
    return <>{children}</>;
  }
  