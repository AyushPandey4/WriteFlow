export const metadata = {
    title: "Blog | WriteFlow",
    description: "Read insightful blogs on WriteFlow.",
  };
  
  export default function BlogLayout({ children }) {
    return <>{children}</>;
  }
  